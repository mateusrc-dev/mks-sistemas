import { styled } from "..";

export const FooterContainer = styled("header", {
  width: "100%",
  height: "34px",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  background: "$gray600",
  marginTop: "auto",
  p: {
    color: "$gray900",
    fontSize: "12px",
    fontWeight: 400,
  },
});